<Window x:Class="LocalTranslatorApp.LanguagePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select language"
        Width="720"
        Height="560"
        MinWidth="560"
        MinHeight="420"
        Icon="Assets/AppIcon.ico"
        Background="#FFFFFF"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1F7AE0" />
        <SolidColorBrush x:Key="TextBrush" Color="#172033" />
        <SolidColorBrush x:Key="MutedBrush" Color="#627083" />
        <SolidColorBrush x:Key="LineBrush" Color="#D7DEE8" />

        <Style TargetType="Button">
            <Setter Property="Height" Value="38" />
            <Setter Property="MinWidth" Value="86" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="#FFFFFF" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
            <RowDefinition Height="58" />
        </Grid.RowDefinitions>

        <Border BorderBrush="#E7ECF2" BorderThickness="0,0,0,1">
            <Grid Margin="22,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="SearchTextBox"
                         Height="40"
                         Padding="12,0"
                         VerticalContentAlignment="Center"
                         BorderBrush="{StaticResource LineBrush}"
                         BorderThickness="1"
                         FontSize="15"
                         TextChanged="SearchTextBox_TextChanged" />
                <Button Grid.Column="1"
                        Content="Clear"
                        Margin="10,0,0,0"
                        Click="ClearSearch_Click" />
            </Grid>
        </Border>

        <ListBox x:Name="LanguageListBox"
                 Grid.Row="1"
                 Margin="14"
                 BorderThickness="0"
                 DisplayMemberPath="Name"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 MouseDoubleClick="LanguageListBox_MouseDoubleClick"
                 KeyDown="LanguageListBox_KeyDown">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="MinHeight" Value="34" />
                    <Setter Property="Padding" Value="12,6" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="FontSize" Value="14" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <Border Grid.Row="2" BorderBrush="#E7ECF2" BorderThickness="0,1,0,0">
            <DockPanel Margin="22,10">
                <TextBlock x:Name="CountTextBlock"
                           DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource MutedBrush}" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Click="Cancel_Click" />
                    <Button Content="Select"
                            Margin="10,0,0,0"
                            Style="{StaticResource PrimaryButton}"
                            Click="Select_Click" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
